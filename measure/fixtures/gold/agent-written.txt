Nothing local needs it — Maven never reads it, and Terraform prefers it over Application Default Credentials (a different auth mechanism which is also used by our Maven gcloud wagon), which quietly broke terraform plan in any shell older than an hour. ADC is a separate grant that the 16-hour reauth does not touch, and ginit now points kubectl at it too, but only where it already exists.
